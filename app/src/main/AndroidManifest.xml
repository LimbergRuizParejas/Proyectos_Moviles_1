<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.aplicacioncliente">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AplicacionCliente"
        tools:targetApi="31"
        android:networkSecurityConfig="@xml/network_security_config">

        <!-- Actividad de inicio de sesión -->
        <activity
            android:name="com.example.aplicacioncliente.ui.login.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Actividad de registro -->
        <activity
            android:name="com.example.aplicacioncliente.ui.login.RegisterActivity"
            android:exported="true" />

        <!-- Actividad de lista de restaurantes -->
        <activity
            android:name="com.example.aplicacincliente.ui.chofer.RestaurantListActivity"
            android:exported="true" />

        <!-- Actividad del menú del restaurante -->
        <activity
            android:name="com.example.aplicacioncliente.ui.chofer.RestaurantMenuActivity"
            android:exported="true" />

        <!-- Otra actividad principal (por ejemplo, chofer o cliente) -->
        <activity
            android:name="com.example.aplicacioncliente.ui.chofer.MainActivity"
            android:exported="true" />
    </application>
</manifest>
